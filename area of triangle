#include<stdio.h>
int main()
{
    float  height,length,area;
    printf("Enter the value of height and length respectively:");
    scanf("%f,%f",&height,&length);
    area=height*(length/2);
    printf("Area of traingle is %f",area);
    return 0;
}
